#include<bits/stdc++.h>
using namespace std;

string s;
int n;

void solve(){
    cin>>s;
    n = s.size();
}

signed main(){
    ios_base::sync_with_stdio(0);
    cin.tie(0);
    cout.tie(0);

    int T;
    cin>>T;

    while(T--){
        solve();
    }

    return 0;
}